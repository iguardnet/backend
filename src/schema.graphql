# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""A custom scalar type for big numbers"""
scalar BigNumber

type ClientInfo {
  android: JSON

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  id: ID!
  ios: JSON
  subscription: JSON

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

type ClientStat {
  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  down: BigNumber!
  email: String!
  enable: Boolean!
  expiryTime: BigNumber!
  id: ID!
  total: BigNumber!
  up: BigNumber!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

type Connection {
  config: String!
  country: ServerCountry!

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  id: ID!
  ip: String!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type FullUser {
  activeGoogleSubscription: GoogleSubscription
  clientInfo: ClientInfo

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  deviceId: String
  email: String
  emailVerified: Boolean
  firebaseId: String
  id: ID!
  name: String
  phone: String
  requestIP: String!
  role: Role!
  signInProvider: SignInProvider

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

input GetClientStatsFiltersInput {
  id: String!
}

type GoogleAllSubscriptions {
  activeGoogleSubscription: GoogleSubscription

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  googleSubscriptions: [GoogleSubscription!]!
  id: ID!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

type GoogleSubscription {
  acknowledgementState: Int!
  autoRenewing: Boolean!
  countryCode: String!

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  developerPayload: String!
  expiryTimeMillis: DateTime!
  id: ID!
  kind: String!
  orderId: String!
  priceAmountMicros: BigNumber!
  priceCurrencyCode: String!
  startTimeMillis: DateTime!
  status: GoogleSubscriptionStatus!
  subscriptionId: String!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

"""Google Subscription Status"""
enum GoogleSubscriptionStatus {
  ACTIVE
  CANCELED_BY_DEVELOPER
  CANCELED_BY_SYSTEM
  CANCELED_BY_USER
  EXPIRED
  FREE_TRIAL
  PAYMENT_PENDING
  PENDING_UPGRADE_DOWNGRADE
  REFUNDED
  REPLACED
  UNKNOWN
}

"""InboundType"""
enum InboundType {
  VLESS_TCP
  VLESS_WS_TLS
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON

"""
A field whose value is a JSON Web Token (JWT): https://jwt.io/introduction.
"""
scalar JWT

type Login {
  loggedIn: LoginData
}

type LoginData {
  tokens: Token!
  user: User!
}

input LoginInput {
  deviceId: String
  firebaseToken: String!
}

type Mutation {
  getConnection(country: String!): Connection!
  login(input: LoginInput!): Login!
  refreshToken(token: JWT!): Token!
  setFirebaseId(input: SetFirebaseIdInput!): String!
  uploadImage(input: UploadInput!): String!
  verifyGoogleSubscription(input: VerifyGoogleSubscriptionInput!): Boolean!
}

type Query {
  clientStats(filters: GetClientStatsFiltersInput!): [ClientStat!]!
  getCustomToken(deviceId: String!): String!
  getUsage: TrafficUsage!
  googleSubscriptions: GoogleAllSubscriptions!
  hello(name: String!): String!
  helloWorld: String!
  me: FullUser!
  servers: [ServerFullInfo!]!
}

"""User role"""
enum Role {
  ADMIN
  USER
}

"""ServerCountry"""
enum ServerCountry {
  DE
  FI
  IR
  NL
  SG
  TR
  US
}

type ServerFullInfo {
  country: String!

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  flagUrl: String!
  id: ID!
  inboundType: InboundType!
  ip: String!
  isPremium: Boolean!
  port: Int!
  tunnelDomain: String!
  type: ServerCountry!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

input SetFirebaseIdInput {
  deviceId: String!
  firebaseId: String!
  firebaseToken: String!
}

"""SignInProvider"""
enum SignInProvider {
  ANONYMOUS
  GOOGLE
  PASSWORD
}

type Token {
  """JWT access token"""
  accessToken: JWT!

  """JWT refresh token"""
  refreshToken: JWT!
}

type TrafficUsage {
  byte: BigNumber!

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  gigabyte: BigNumber!
  id: ID!
  isLimitReached: Boolean!
  megabyte: BigNumber!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload

input UploadInput {
  image: Upload!
}

type User {
  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  deviceId: String
  email: String
  emailVerified: Boolean
  firebaseId: String
  id: ID!
  name: String
  phone: String
  role: Role!
  signInProvider: SignInProvider

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

input VerifyGoogleSubscriptionInput {
  packageName: String!
  purchaseToken: String!
  subscriptionId: String!
}