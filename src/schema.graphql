# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""A custom scalar type for big numbers"""
scalar BigNumber

type ClientInfo {
  android: JSON

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  id: ID!
  ios: JSON
  subscription: JSON

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

type ClientStat {
  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  down: BigNumber!
  email: String!
  enable: Boolean!
  expiryTime: BigNumber!
  id: ID!
  total: BigNumber!
  up: BigNumber!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

type Connection {
  config: String!
  country: ServerCountry!

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  id: ID!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input GetClientStatsFiltersInput {
  id: String!
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON

"""
A field whose value is a JSON Web Token (JWT): https://jwt.io/introduction.
"""
scalar JWT

type Login {
  loggedIn: LoginData
}

type LoginData {
  tokens: Token!
  user: User!
}

input LoginInput {
  deviceId: String
  firebaseToken: String!
}

type Mutation {
  getConnection(country: String!): Connection!
  login(input: LoginInput!): Login!
  refreshToken(token: JWT!): Token!
  setFirebaseId(input: SetFirebaseIdInput!): String!
  uploadImage(input: UploadInput!): String!
}

type Query {
  clientStats(filters: GetClientStatsFiltersInput!): [ClientStat!]!
  getCustomToken(deviceId: String!): String!
  getUsage: TrafficUsage!
  hello(name: String!): String!
  helloWorld: String!
  me: UserWithClientInfo!
  servers: [ServerFullInfo!]!
}

"""User role"""
enum Role {
  ADMIN
  PRO_USER
  USER
}

"""ServerCountry"""
enum ServerCountry {
  DE
  FI
  IR
  NL
  SG
  TR
  US
}

type ServerFullInfo {
  country: String!

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  flagUrl: String!
  id: ID!
  ip: String!
  isPremium: Boolean!
  type: ServerCountry!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

input SetFirebaseIdInput {
  deviceId: String!
  firebaseId: String!
  firebaseToken: String!
}

"""SignInProvider"""
enum SignInProvider {
  ANONYMOUS
  GOOGLE
  PASSWORD
}

type Token {
  """JWT access token"""
  accessToken: JWT!

  """JWT refresh token"""
  refreshToken: JWT!
}

type TrafficUsage {
  byte: BigNumber!

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  gigabyte: BigNumber!
  id: ID!
  megabyte: BigNumber!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

"""The `Upload` scalar type represents a file upload."""
scalar Upload

input UploadInput {
  image: Upload!
}

type User {
  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  deviceId: String
  email: String
  emailVerified: Boolean
  firebaseId: String
  id: ID!
  name: String
  phone: String
  role: Role!
  signInProvider: SignInProvider

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}

type UserWithClientInfo {
  clientInfo: ClientInfo

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  deviceId: String
  email: String
  emailVerified: Boolean
  firebaseId: String
  id: ID!
  name: String
  phone: String
  role: Role!
  signInProvider: SignInProvider

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}